[project]
name = "cm-interview"
version = "0.0.0"
description = "Local-first DevOps interview exercise"
requires-python = ">=3.12,<3.14"
dependencies = []

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"
