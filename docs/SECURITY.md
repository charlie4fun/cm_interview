# Security

- Keep credentials and tokens out of the repository.
- Use pre-commit checks to detect accidentally committed secrets.
- Give GitHub Actions the minimum required permissions.
- Report vulnerabilities through GitHub private vulnerability reporting as described in the root `SECURITY.md`.
